# 아이폰에서 GitHub에 올리는 방법

GitHub 저장소 화면에서:

1. `Add file`
2. `Upload files`
3. 파일 앱에서 압축을 푼 폴더 안의 파일들을 선택
4. `Commit changes`

`functions/api/extract.js`는 폴더 구조가 유지되어야 합니다.
모바일 웹에서 폴더 업로드가 되지 않으면 다음 순서로 직접 만드세요.

1. 저장소에서 `Add file` → `Create new file`
2. 파일 이름 칸에 `functions/api/extract.js`를 그대로 입력
3. 제공된 `extract.js` 내용을 붙여넣고 Commit
4. 나머지 네 파일은 Upload files로 올리기
